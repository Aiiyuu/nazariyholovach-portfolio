import { Variants } from "framer-motion";

export const adaptiveMenuBtnVariatns: Variants = {
  hove: { scale: 1.1 },
  tap: { scale: 1.2 },
};

export const closeAdaptiveMenuBtnVariatns: Variants = {
  hover: { scale: 1.1 },
  tap: { scale: 1.2 },
};
