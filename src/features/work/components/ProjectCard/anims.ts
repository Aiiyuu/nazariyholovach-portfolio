import { Transition, Variants } from "framer-motion";

export const thumbnailVariants: Variants = {
  initial: { scale: 1 },
  hover: { scale: 1.2 },
};

export const transition: Transition = { duration: 10, ease: "easeInOut" };
